<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>finance.idem</groupId>
        <artifactId>idem</artifactId>
        <version>0.1.0-SNAPSHOT</version>
    </parent>

    <artifactId>app</artifactId>
    <name>idem-app</name>
    <description>Spring Boot entry point — wires all modules together</description>

    <dependencies>
        <!-- Internal modules (transitive deps bring in everything) -->
        <dependency>
            <groupId>finance.idem</groupId>
            <artifactId>api</artifactId>
        </dependency>
        <dependency>
            <groupId>finance.idem</groupId>
            <artifactId>mcp</artifactId>
        </dependency>
        <dependency>
            <groupId>finance.idem</groupId>
            <artifactId>infrastructure</artifactId>
        </dependency>

        <!-- Actuator + Spring Modulith runtime -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-actuator</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-observability</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Local dev: auto-starts services from compose.yaml -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-docker-compose</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.modulith</groupId>
            <artifactId>spring-modulith-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>finance.idem</groupId>
            <artifactId>idem-sdk-kotlin</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- spring-boot-maven-plugin lives ONLY in app — no other module gets an executable jar -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <executions>
                    <!-- Writes META-INF/build-info.properties so BuildProperties bean is available
                         for TelemetryPingService to report the running idem version. -->
                    <execution>
                        <id>build-info</id>
                        <goals>
                            <goal>build-info</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!--
          Activated explicitly with -P generate-openapi. Boots the full app (requires a live
          Postgres + Redis, e.g. via `docker-compose up -d`) and hits its /api-docs endpoint to
          regenerate the repo-root openapi.yaml. Not part of the default build/CI lifecycle —
          run locally and commit the resulting openapi.yaml as part of the PR:

            docker-compose up -d
            ./mvnw -P generate-openapi verify -pl app -am
        -->
        <profile>
            <id>generate-openapi</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>pre-integration-test</id>
                                <goals>
                                    <goal>start</goal>
                                </goals>
                                <configuration>
                                    <!-- spring-boot-docker-compose looks for compose.yaml relative to
                                         the process working directory (this module's basedir), where
                                         none exists — it only lives at the repo root. Disable it here;
                                         the documented workflow already requires `docker-compose up -d`
                                         to be run manually before this profile. -->
                                    <jvmArguments>-Dspring.docker.compose.enabled=false -Dspring.profiles.active=dev</jvmArguments>
                                </configuration>
                            </execution>
                            <execution>
                                <id>post-integration-test</id>
                                <goals>
                                    <goal>stop</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.springdoc</groupId>
                        <artifactId>springdoc-openapi-maven-plugin</artifactId>
                        <version>1.4</version>
                        <executions>
                            <execution>
                                <phase>integration-test</phase>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <apiDocsUrl>http://localhost:8081/api-docs.yaml</apiDocsUrl>
                            <outputFileName>openapi.yaml</outputFileName>
                            <outputDir>${project.basedir}/../</outputDir>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <!--
          Activated explicitly with -P native. Enables both:
            ./mvnw -Pnative native:compile        (standalone binary, used by CI + Dockerfile.native)
            ./mvnw -Pnative spring-boot:build-image (Paketo buildpacks native container image)
          native-maven-plugin version is managed by the spring-boot-starter-parent BOM.
        -->
        <profile>
            <id>native</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>process-aot</id>
                                <goals>
                                    <goal>process-aot</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.graalvm.buildtools</groupId>
                        <artifactId>native-maven-plugin</artifactId>
                        <configuration>
                            <mainClass>finance.idem.IdemApplicationKt</mainClass>
                            <!-- Deterministic output name — without this it defaults to
                                 project.build.finalName (e.g. app-0.1.0-SNAPSHOT), which the
                                 native-build.yml CI job and Dockerfile.native both assume is "idem". -->
                            <imageName>idem</imageName>
                            <buildArgs>
                                <!-- Fully static binary linked against musl — required to run in the
                                     FROM scratch base image in Dockerfile.native (no libc present). -->
                                <buildArg>--static</buildArg>
                                <buildArg>--libc=musl</buildArg>
                            </buildArgs>
                        </configuration>
                        <executions>
                            <execution>
                                <id>add-reachability-metadata</id>
                                <goals>
                                    <goal>add-reachability-metadata</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>

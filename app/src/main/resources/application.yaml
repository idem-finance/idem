spring:
  application:
    name: idem
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

server:
  port: 8081

idem:
  audit:
    # Must be set to a strong secret in production — rotate via secret manager
    hmac-secret: dev-only-insecure-change-in-production

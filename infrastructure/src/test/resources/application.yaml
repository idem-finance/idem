spring:
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false

idem:
  audit:
    hmac-secret: test-only-insecure-hmac-secret
  chain:
    alchemy-webhook-signing-key: "test-only-insecure-alchemy-key"
    quicknode-webhook-secret: "test-only-insecure-quicknode-secret"
